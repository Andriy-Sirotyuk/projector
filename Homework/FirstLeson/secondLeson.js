"use strict";


/*

const userPassword = prompt("Введіть пароль:");
const isValid = userPassword.length >= 8 && !isNaN(userPassword[0]);

console.log(isValid);
*/


/*
2. Конвертер валют

const rate = parseFloat(prompt("Введіть поточний курс долара до гривні:"));
const amountInUAH = parseFloat(prompt("Введіть суму в гривнях для конвертації:"));

if (isNaN(rate) || isNaN(amountInUAH)) {
    console.log("Будь ласка, введіть коректні числові значення.");
} else {
    const amountInUSD = amountInUAH / rate;
    console.log(`Сума в доларах: $${amountInUSD.toFixed(2)}`);
}
*/


/*
3. Обчислення вартості бензину

const liters = parseFloat(prompt("Введіть кількість літрів бензину:"));
const pricePerLiter = parseFloat(prompt("Введіть ціну за літр бензину:"));

if (isNaN(liters) || isNaN(pricePerLiter)) {
    console.log("Будь ласка, введіть коректні числові значення.");
} else {
    const totalCost = liters * pricePerLiter;
    console.log(`Вартість покупки: $${totalCost.toFixed(2)}`);
}
*/



